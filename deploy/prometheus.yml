global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: bastion
    static_configs:
      - targets: ["bastion:8080"]
    metrics_path: /metrics
    # Bastion exposes a standard Prometheus /metrics endpoint.
